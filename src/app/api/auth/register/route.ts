import { NextResponse } from 'next/server'; import { getDB } from '../../../../lib/d1';
async function hash(value:string){const data=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return Array.from(new Uint8Array(data)).map(x=>x.toString(16).padStart(2,'0')).join('')}
export async function POST(request:Request){const body=await request.json().catch(()=>null);if(!body?.email||!body?.password||!body?.name||body.password.length<8)return NextResponse.json({error:'邮箱、昵称和至少8位密码为必填项'},{status:400});const db=getDB();if(!db)return NextResponse.json({error:'D1 未绑定'},{status:503});const id=crypto.randomUUID();try{await db.prepare('INSERT INTO users (id,email,name,password_hash,created_at) VALUES (?,?,?,?,?)').bind(id,body.email.toLowerCase(),body.name,await hash(body.password),Date.now()).run();return NextResponse.json({id,name:body.name})}catch{return NextResponse.json({error:'邮箱已注册'},{status:409})}}
